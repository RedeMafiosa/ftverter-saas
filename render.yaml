services:
  - type: web
    name: ftverter-saas
    env: node
    plan: free
    region: frankfurt
    buildCommand: npm install
    startCommand: node server/server.js
    autoDeploy: true

    envVars:
      - key: NODE_VERSION
        value: 18